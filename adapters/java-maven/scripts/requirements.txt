# Tier-2 (Stage A) parser deps for java_ast.py. MIT-licensed, free/OSS.
# Full Java 21 grammar (records, sealed types, switch expressions, text blocks).
# If unavailable, the adapter falls back to grep and records a CAPABILITY gap.
tree-sitter>=0.21
tree-sitter-java>=0.21
